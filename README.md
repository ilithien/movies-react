# Movie application with OMBDb API

Initialization steps: 

## 1. Install dependencies

`yarn` or `npm install`

Yarn was used during development phase.

## 2. Start application

`yarn start`

## (Optional) 3. Run tests

`yarn test`
